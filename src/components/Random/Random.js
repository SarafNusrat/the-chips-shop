import React from 'react';
import Cart from '../Cart/Cart';

const Random = ({random}) => {
    return (
        <div>
            {/* <h1>{random}</h1>
            <p>{random}</p> */}
        </div>
    );
};

export default Random;